; Module loading. res_resolver_unbound is broken inside this container (libunbound chokes on
; the host's /etc/hosts entries and then ALL DNS fails -> "Failed to resolve sip.telnyx.com").
; Disabling it makes Asterisk fall back to the normal system resolver, which works fine.
[modules]
autoload=yes
noload => res_resolver_unbound.so
