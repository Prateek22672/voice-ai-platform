; PJSIP config — SIP trunk (Telnyx) + local softphone for testing
[global]
type=global
endpoint_identifier_order=ip,username,anonymous

[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060

; ---- Telnyx trunk (credentials injected from .env by entrypoint.sh at container start) ----
[telnyx-trunk]
type=endpoint
context=inbound
disallow=all
allow=ulaw,alaw,slin16
outbound_auth=telnyx-auth
aors=telnyx-aor
from_domain=sip.telnyx.com
from_user=CHANGE_ME_DID          ; caller ID = your purchased Telnyx number (E.164)
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes

[telnyx-auth]
type=auth
auth_type=userpass
username=CHANGE_ME_SIP_USER
password=CHANGE_ME_SIP_PASS

[telnyx-aor]
type=aor
contact=sip:sip.telnyx.com

[telnyx-identify]
type=identify
endpoint=telnyx-trunk
match=sip.telnyx.com

; ---- local softphone for Phase 0 testing (register any SIP client, $0 cost) ----
[softphone]
type=endpoint
context=inbound
disallow=all
allow=ulaw,slin16
auth=softphone-auth
aors=softphone-aor

[softphone-auth]
type=auth
auth_type=userpass
username=softphone
password=test1234

[softphone-aor]
type=aor
max_contacts=1
