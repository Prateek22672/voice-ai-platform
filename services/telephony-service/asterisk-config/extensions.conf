; All inbound calls -> Stasis app "voiceai" (handled by ari_app.py)
[inbound]
exten => _X.,1,NoOp(Inbound call to ${EXTEN})
 same => n,Stasis(voiceai)
 same => n,Hangup()

exten => _+X.,1,NoOp(Inbound E164 ${EXTEN})
 same => n,Stasis(voiceai)
 same => n,Hangup()
