FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y asterisk && rm -rf /var/lib/apt/lists/*
COPY services/telephony-service/asterisk-config/ /etc/asterisk/
EXPOSE 5060/udp 8088/tcp 10000-10100/udp
CMD ["asterisk", "-f", "-vvv"]
