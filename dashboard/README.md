# dashboard
- `public/voice-client.html` — zero-build browser voice test client. Open directly or serve via Next.
- Next.js app: overview, API key creation, usage viewer.
```bash
npm install && npm run dev   # http://localhost:3000
```
